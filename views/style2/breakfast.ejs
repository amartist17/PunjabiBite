<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Breakfast</title>
    <link rel="stylesheet" href="/css/breakfast.css" />
  </head>
  <body>
    <section
      class="page"
      style="background-image: url(/images/BreakfastBg.png)"
    >
      <h1>@<%= price %></h1>
      <div class="data">
        <ul>
          <% for(i = 0;i<items.length;i++) { %>
            <% if(i%2==0) {%> 
          <li><%= items[i] %></li>
          <% } %>
          <% } %>
        </ul>
        <ul>
          <% for(i = 0;i<items.length;i++) { %>
            <% if(i%2!=0) {%> 
          <li><%= items[i] %></li>
          <% } %>
          <% } %>
        </ul>
      </div>
    </section>
  </body>
</html>
